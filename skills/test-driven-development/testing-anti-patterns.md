# 测试反模式

**在以下情况下加载此参考：** 编写或更改测试、添加模拟，或被诱惑向生产代码添加仅测试方法。

## 概述

测试必须验证真实行为，而不是模拟行为。模拟是隔离的手段，而不是被测试的东西。

**核心原则：** 测试代码做什么，而不是模拟做什么。

**遵循严格的 TDD 可以防止这些反模式。**

## 铁律

```
1. 永不测试模拟行为
2. 永不向生产类添加仅测试方法
3. 永不不理解依赖就模拟
```

## 反模式 1：测试模拟行为

**违规：**
```typescript
// ❌ 坏：测试模拟存在
test('renders sidebar', () => {
  render(<Page />);
  expect(screen.getByTestId('sidebar-mock')).toBeInTheDocument();
});
```

**为什么这是错的：**
- 您在验证模拟工作，而不是组件工作
- 模拟存在时测试通过，不存在时失败
- 没有告诉您真实行为

**您的搭档的纠正：** "我们是在测试模拟的行为吗？"

**修复：**
```typescript
// ✅ 好：测试真实组件或不模拟它
test('renders sidebar', () => {
  render(<Page />);  // 不要模拟 sidebar
  expect(screen.getByRole('navigation')).toBeInTheDocument();
});

// 或者如果必须模拟 sidebar 以实现隔离：
// 不要对模拟断言 - 测试 sidebar 存在时 Page 的行为
```

### 门函数

```
在断言任何模拟元素之前：
  问："我是在测试真实组件行为还是只是模拟存在？"

  如果是测试模拟存在：
    停止 - 删除断言或取消模拟组件

  改为测试真实行为
```

## 反模式 2：生产中的仅测试方法

**违规：**
```typescript
// ❌ 坏：destroy() 仅在测试中使用
class Session {
  async destroy() {  // 看起来像生产 API！
    await this._workspaceManager?.destroyWorkspace(this.id);
    // ... cleanup
  }
}

// 在测试中
afterEach(() => session.destroy());
```

**为什么这是错的：**
- 生产类被仅测试代码污染
- 如果在生产中意外调用会很危险
- 违反 YAGNI 和关注点分离
- 混淆对象生命周期和实体生命周期

**修复：**
```typescript
// ✅ 好：测试工具处理测试清理
// Session 没有 destroy() - 在生产中是无状态的

// 在 test-utils/
export async function cleanupSession(session: Session) {
  const workspace = session.getWorkspaceInfo();
  if (workspace) {
    await workspaceManager.destroyWorkspace(workspace.id);
  }
}

// 在测试中
afterEach(() => cleanupSession(session));
```

### 门函数

```
在向生产类添加任何方法之前：
  问："这仅被测试使用吗？"

  如果是：
    停止 - 不要添加它
    改为放在测试工具中

  问："这个类拥有这个资源的生命周期吗？"

  如果不是：
    停止 - 这个方法放错了类
```

## 反模式 3：不理解就模拟

**违规：**
```typescript
// ❌ 坏：模拟破坏测试逻辑
test('detects duplicate server', () => {
  // 模拟阻止了测试依赖的配置写入！
  vi.mock('ToolCatalog', () => ({
    discoverAndCacheTools: vi.fn().mockResolvedValue(undefined)
  }));

  await addServer(config);
  await addServer(config);  // 应该抛出 - 但不会！
});
```

**为什么这是错的：**
- 模拟的方法有测试依赖的副作用（写入配置）
- 过度模拟"为了安全"破坏实际行为
- 测试因错误原因通过或神秘失败

**修复：**
```typescript
// ✅ 好：在正确层级模拟
test('detects duplicate server', () => {
  // 模拟慢的部分，保留测试需要的行为
  vi.mock('MCPServerManager'); // 只模拟慢的服务器启动

  await addServer(config);  // 配置被写入
  await addServer(config);  // 检测到重复 ✓
});
```

### 门函数

```
在模拟任何方法之前：
  停止 - 暂时不要模拟

  1. 问："真实方法有什么副作用？"
  2. 问："这个测试依赖这些副作用中的任何一个吗？"
  3. 问："我完全理解这个测试需要什么吗？"

  如果依赖副作用：
    在更低层级模拟（实际的慢/外部操作）
    或使用保留必要行为的测试替身
    而不是测试依赖的高级方法

  如果不确定测试依赖什么：
    先用真实实现运行测试
    观察实际需要发生什么
    然后在正确层级添加最小模拟

  红旗：
    - "我会模拟这个以确保安全"
    - "这可能很慢，最好模拟它"
    - 不理解依赖链就模拟
```

## 反模式 4：不完整的模拟

**违规：**
```typescript
// ❌ 坏：部分模拟 - 只有您认为需要的字段
const mockResponse = {
  status: 'success',
  data: { userId: '123', name: 'Alice' }
  // 缺少：下游代码使用的 metadata
};

// 稍后：当代码访问 response.metadata.requestId 时崩溃
```

**为什么这是错的：**
- **部分模拟隐藏结构假设** - 您只模拟了您知道的字段
- **下游代码可能依赖您没有包含的字段** - 静默失败
- **测试通过但集成失败** - 模拟不完整，真实 API 完整
- **虚假信心** - 测试没有证明任何关于真实行为的事情

**铁律：** 模拟完整的真实存在的数据结构，而不只是您 immediate 测试使用的字段。

**修复：**
```typescript
// ✅ 好：反映真实 API 的完整性
const mockResponse = {
  status: 'success',
  data: { userId: '123', name: 'Alice' },
  metadata: { requestId: 'req-789', timestamp: 1234567890 }
  // 真实 API 返回的所有字段
};
```

### 门函数

```
在创建模拟响应之前：
  检查："真实 API 响应包含什么字段？"

  操作：
    1. 从文档/示例检查实际 API 响应
    2. 包含系统可能在下游消费的所有字段
    3. 验证模拟完全匹配真实响应模式

  关键：
    如果您正在创建模拟，您必须理解整个结构
    当代码依赖省略的字段时，部分模拟会静默失败

  如果不确定：包含所有记录的字段
```

## 反模式 5：集成测试马后炮

**违规：**
```
✅ 实现完成
❌ 没有写测试
"准备测试"
```

**为什么这是错的：**
- 测试是实现的一部分，而不是可选的后续
- TDD 会发现这个
- 没有测试就不能声称完成

**修复：**
```
TDD 周期：
1. 写失败的测试
2. 实现通过
3. 重构
4. 然后才声称完成
```

## 当模拟变得太复杂时

**警告信号：**
- 模拟设置比测试逻辑长
- 模拟一切使测试通过
- 模拟缺少真实组件有的方法
- 当您移除模拟时测试失败

**您搭档的问题：** "我们这里需要使用模拟吗？"

**考虑：** 使用真实组件的集成测试通常比复杂模拟更简单

## TDD 防止这些反模式

**为什么 TDD 有帮助：**
1. **先写测试** → 强制您思考您实际在测试什么
2. **观察它失败** → 确认测试测试真实行为，而不是模拟
3. **最小实现** → 不会有仅测试方法混入
4. **真实依赖** → 在模拟之前您看到测试实际需要什么

**如果您在测试模拟行为，您违反了 TDD** - 您在没有先观察测试针对真实代码失败的情况下添加了模拟。

## 快速参考

| 反模式 | 修复 |
|--------------|-----|
| 对模拟元素断言 | 测试真实组件或取消模拟 |
| 生产中的仅测试方法 | 移到测试工具 |
| 不理解就模拟 | 先理解依赖，最小化模拟 |
| 不完整的模拟 | 完全反映真实 API |
| 测试马后炮 | TDD - 测试优先 |
| 过度复杂的模拟 | 考虑集成测试 |

## 红旗

- 断言检查 `*-mock` 测试 ID
- 方法仅在测试文件中调用
- 模拟设置占测试的 >50%
- 当您移除模拟时测试失败
- 无法解释为什么需要模拟
- "只是为了安全"而模拟

## 底线

**模拟是隔离的工具，而不是被测试的东西。**

如果 TDD 揭示您在测试模拟行为，您就做错了。

修复：测试真实行为或质疑您为什么模拟。
